---
title: Articles
description: "Curated learning notes, insights and perspective, book and technology reviews
    related to full-stack web application development and delivery
    with cloud service infrastructure."
---

{% include '_content/page-header.njk' %}

<section class="page-content">
   <div class="page-content__details">
        <section class="article-list">
            {% for post in collections.articles | sortedCollectionByDateDesc %}
            <article class="article-list__item">
                <a href="{{ post.url }}" class="article-list__item-title">{{ post.data.title }}</a>
                <div class="article-list__item-meta small">
                    <span><strong>{{ post.data.category }}</strong></span>
                    <span class="divider"> | </span>
                    <span>Published on {{ post.data.date | localeDate }}</span>
                </div>
                <a href="{{ post.url }}" class="article-list__item-excerpt">
                    {% if post.data.description %}
                        {{ post.data.description | excerpt }}
                    {% else %}
                        {{ post.templateContent | excerpt }}
                    {% endif %}
                </a>
            </article>
            {% endfor %}
        </section>
    </div>
    <div class="page-content__sidebar cards cards-gray">
        {% include '_content/article-category-list.njk' %}
    </div>
    <div class="page-content__sidebar">
        {% include '_content/article-subcategory-list.njk' %}
    </div>
    <div class="page-content__sidebar">
        <small>[ Support and Donation ]</small>
    </div>
</section>