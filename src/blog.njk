---
title: Blog
description: "Welcome to my blog! Here, I'll share my thoughts and insights on various topics.
    Join me in exploring new ideas and perspectives through my learning journet and experience working with
    full-stack web application development delivery inluding software engineering as a whole."
permalink: /blog/{% if pagination.pageNumber > 0 %}page-{{ pagination.pageNumber + 1 }}/{% endif %}
calloutFontAwesomeIcon: fa-comments
pagination:
    data: collections.blogPosts
    size: 3
    alias: pagedBlogPosts
---

{% include '_page-callout.njk' %}
{% include '_content/recent-blog-posts.njk' %}

{# <section class="container recent-blog">
    <aside  class="recent-blog__sidebar">
        <div class="recent-blog__categories">
            <h6>Categories</h6>
            <ul>
            {% for category in collections.blogByCategory %}
                <li>{{ category[0] }} ({{ category[1].length }})</li>
            {% endfor %}
            </ul>
        </div>
    </aside>
    <article class="recent-blog__list">
        <ul>
        {% for post in pagedBlogPosts | sort(false, false, date) %}
            <li>
                <a href="{{ post.url }}" class="recent-blog__title">{{ post.data.title }}</a>
                <div class="recent-blog__meta small">Published on {{ post.data.date | postDate }}</div>
                <a href="{{ post.url }}" class="recent-blog__excerpt">
                    {{ post.templateContent | excerpt }}
                </a>
            </li>
        {% endfor %}
        </ul>

        {% if pagination.href.previous or pagination.href.previous %}
        <nav class="recent-blog__page-nav">
            {% if pagination.href.previous %}
            <a href="{{ pagination.href.previous }}" class="page-nav page-nav__previous">
                <i class="fa-solid fa-angles-left fa-2x"></i>
                <span>PREVIOUS</span>
            </a>
            {%else%}<small>Click 'Next' for more recent posts...</small>{% endif %}

            {% if pagination.href.next %}
            <a href="{{ pagination.href.next }}" class="page-nav page-nav__next">
                <span>NEXT</span>
                <i class="fa-solid fa-angles-right fa-2x"></i>
            </a>
            {%else%}<small>You've reaced the end of the list!</small>{% endif %}
        </nav>
        {% endif %}
    </article>
</section> #}